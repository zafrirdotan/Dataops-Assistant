# Pipeline Environment Configuration
# Data Directories
DATA_FOLDER=../../data
OUTPUT_FOLDER=./output
# LOG_FOLDER=./logs

# # Database Configuration (PostgreSQL)
DATABASE_URL=postgresql://dataops_user:dataops_password@localhost:5432/dataops_db
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=dataops_db
DATABASE_USER=dataops_user
DATABASE_PASSWORD=dataops_password

# # MinIO Configuration
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_SECURE=false
MINIO_BUCKET=dataops-bucket

# # Pipeline Execution Configuration
# EXECUTION_MODE=local
# LOG_LEVEL=INFO
# MAX_RETRIES=3
# TIMEOUT_SECONDS=300

# # Optional: API Configuration
# API_BASE_URL=http://localhost:8000
# API_TIMEOUT=30

# # Optional: Spark Configuration (if using Spark)
# SPARK_MASTER=local[*]
# SPARK_APP_NAME=DataOps-Pipeline

# # Optional: Airflow Configuration (if using Airflow)
# AIRFLOW_HOME=./airflow
