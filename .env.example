OPENAI_API_KEY=your_openai_api_key_here

# JWT Authentication Configuration
# Generate a secure SECRET_KEY using: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-change-this-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
COOKIE_SECURE=false

# Environment Configuration
# Options: prod, dev, local-debug
# - prod/production: Uses AWS S3 for storage
# - dev/development: Uses MinIO for storage
# - local-debug/local: Uses local filesystem for storage (debugging only)
ENVIRONMENT=dev

# Volume absolute paths on the host machine
HOST_DATA_PATH={your_absolute_path_to_the_project}/Dataops-Assistant/data
HOST_OUTPUT_PATH={your_absolute_path_to_the_project}/Dataops-Assistant/output
HOST_PIPELINES_PATH={your_absolute_path_to_the_project}/Dataops-Assistant/pipelines

# S3 Configuration
# For MinIO (local development) - leave S3_ENDPOINT with MinIO URL
# For AWS S3 (production) - set S3_ENDPOINT to empty string or "none"
S3_ENDPOINT=http://minio:9000
S3_REGION=us-east-1
S3_BUCKET=dataops-bucket
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_USE_PATH_STYLE=true
PUBLIC_S3_BASE_URL=http://localhost:9000
PRESIGN_EXPIRES_SECONDS=600

# Example AWS S3 Configuration (uncomment and update for production):
# S3_ENDPOINT=
# S3_REGION=us-east-1
# S3_BUCKET=your-aws-bucket-name
# S3_ACCESS_KEY=your-aws-access-key
# S3_SECRET_KEY=your-aws-secret-key
# S3_USE_PATH_STYLE=false
# PUBLIC_S3_BASE_URL=https://your-aws-bucket-name.s3.amazonaws.com
